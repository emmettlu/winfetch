- 不写多余的 md 文件
- 别重复造轮子
- 尽量不调用系统shell工具修改文件, 而是直接修改文件
- 除 git 的 commit message 使用英文之外, 其他所有输出, 说明, 注释均使用简体中文
- git 的 commit message 只需一段即可 
- 所有标点符号都使用半角(英文)而非全角(中文), 逗号后追加一个空格
- 不用 release 模式测试构建, 前期不在意性能
- 写完只需 cargo check or cargo clippy 即可, 无需运行, 我来手动运行
